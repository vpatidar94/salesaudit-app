import { Point } from './point';
export declare class PointPool {
    private borrowed;
    private firstAvailable;
    constructor(initialSize?: number);
    readonly instance: PointPool;
    borrow(x: number, y: number): Point;
    returnPoint(p: Point): void;
}
