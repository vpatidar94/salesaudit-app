export declare class ImageCropperDataShare {
    share: any;
    pressed: HTMLCanvasElement;
    over: HTMLCanvasElement;
    setPressed(canvas: HTMLCanvasElement): void;
    setReleased(canvas: HTMLCanvasElement): void;
    setOver(canvas: HTMLCanvasElement): void;
    setStyle(canvas: HTMLCanvasElement, style: any): void;
}
